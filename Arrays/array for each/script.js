// const names = ["oanh", "ngoc", "lan", "hoa"];
// names.forEach((name, index) => {
//   console.log(name, index);
// });

let sum = 0;
const numbers = [1, 2, 3, 4, 5, 6];
numbers.forEach((number, i) => (sum += number));
console.log(sum);
